[project]
name = "asl-anki-generator"
version = "0.1.0"
description = "Generate Anki flashcard decks for ASL vocabulary"
requires-python = ">=3.13"
dependencies = [
  "genanki",
  "pydantic",
  "requests",
]

[dependency-groups]
dev = [
  "pytest",
]
